// Misleading filename: actually loads feed posters.
export function loadPublishedPosters(): string[] {
  return ["poster-1", "poster-2"];
}
export function toPlainPoster(p: string): string {
  return p;
}
