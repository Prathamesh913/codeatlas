// S7 — multi-flag: automation directory + *.test filename.
// Precedence: test wins as primary class; automation flag must be preserved.
export const uploadFixture = { name: "poster upload test fixture" };
