// S2 — tests are relevance class `test`.
import { describe, it } from 'node:test';

describe('search', () => {
  it('finds posters by title', () => {
    // test assertions are not product capabilities
  });
});
